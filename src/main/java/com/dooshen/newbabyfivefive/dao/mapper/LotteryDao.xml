<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dooshen.newbabyfivefive.dao.LotteryDao">

		<select id="findLotteryByExpect" parameterType="java.util.String">
		select *  from lottory where expect=#{expect}
		</select>
</mapper>